# 隐私与公开发布范围

“保持清醒”在本机使用 macOS 电源管理接口。当前应用源码不包含网络请求、广告、分析统计、账户登录、远程日志、设备标识采集或自动更新功能。它不读取 Wi-Fi 名称、公共网络登录信息或 Agent 的工作内容。

应用会在本机保存首次使用说明是否已显示，以及系统负责管理的菜单栏显示偏好。用户主动选择“登录时打开”时，由 macOS 管理登录项。防休眠状态属于当前应用进程，关闭或退出应用会释放该状态。

## 此仓库公开什么

公开内容限于通用应用源码、构建与验证脚本、说明文档、测试源码和原创图示。项目中的用户名应使用占位符或当前用户环境变量，不应包含作者的本机目录、邮箱、证书、私钥、个人运行日志或调试截图。

仓库链接中公开的 GitHub 用户名，以及 GitHub 本身公开显示的贡献者身份，是发布项目所必需的公开元数据。它们不应被误认为应用在收集个人信息。发布者还应检查 Git 提交身份：平台生成的隐私保护提交邮箱可以隐藏私人邮箱，但不能隐藏仓库的公开所有者。

目前采用源码发布：`build/`、`dist/`、本机构建缓存和本机安装的 `.app` 不属于默认上传范围。未经 Developer ID 签名及 Apple 公证的本机应用不能被描述为已通过正式分发验证。

## 发布前检查

在仓库根目录运行：

```sh
python3 scripts/privacy_check.py
```

检查脚本只读取明确允许公开的根文件，以及 `Sources/`、`Tests/`、`scripts/`、`docs/`、`assets/` 和 `.github/workflows/`。它检测常见的本机路径、私人邮箱、访问令牌、私钥片段、个人应用标识和运行编号，并报告文件、行号及类别，不回显命中的值。遇到凭据或证书命名的文件只报告文件名，不读取其内容。

脚本不会跟随符号链接，不默认检查构建输出，也不会自动解包归档。需要检查未来准备公开的未压缩构建产物时，显式运行：

```sh
python3 scripts/privacy_check.py --include-build --include-dist
```

二进制检查可以发现部分明文路径或身份信息，不能证明二进制完全不含个人信息。签名证书中的开发者姓名属于公开分发身份；正式发行前应由发布者确认这个身份可以公开。

自动检查是人工审查的辅助，不等于完整的秘密信息扫描或匿名性保证。上传前还需要确认实际待发布文件与公开白名单一致，核对 Git 提交身份，检查文档、许可证和图片中的署名及元数据，并确保没有个人日志、截图、凭据或本机构建产物混入上传文件。
