# 给本机 Agent 的安装指南

这个项目当前发布源码，安装需要在用户自己的 Mac 上构建。目标是完成一个可验证的本机安装，不是让用户下载一个未知来源的现成应用。

## 1. 阅读与检查

先阅读 README.md、Sources/、Info.plist，以及 scripts/install.zsh、build.sh、common.sh、verify-app.sh。应用只能管理自己的防闲置睡眠状态；不应访问用户的 Wi-Fi、账号、文件、浏览器或 Agent 工作内容。

使用 GitHub 的源码 ZIP 或正常 git clone 获取仓库。将项目保存在用户可写的独立目录，不要将远程脚本直接通过管道交给 shell 执行。

## 2. 检测环境

- 必须是 Mac；检查系统版本与架构。
- 检查 `xcode-select -p`、`xcrun --find swiftc`。本地构建目标为 macOS 13+，同时产生 arm64 和 x86_64 架构；编译器本身也有系统兼容性要求。
- 缺少工具时，解释 Apple Command Line Tools 的用途，提示用户通过 `xcode-select --install` 的系统界面完成安装。不要绕过系统协议、安全提示或凭据要求。
- 安装前查看是否已有同名应用、是否正在运行。先让用户正常退出要更新的同一应用；不自动终止其他程序。

## 3. 构建并安装

在仓库根目录运行：

```sh
zsh scripts/install.zsh
```

脚本自行构建和验证，默认安装到当前用户的 `~/Applications`，不需要 sudo，也不会自动启动。如果未知同名应用导致拒绝，解释冲突，使用其他明确的目标目录；不要盲目删除或覆盖旧应用。

对于具备界面工具的 Agent，请使用其界面工具打开安装后的应用。没有界面工具时，可请用户在 Finder 双击，或在其授权范围内使用安装脚本的 `--launch` 选项。

## 4. 验证真实结果

1. 首次启动默认关闭，图标应为带斜杠的杯子和“关”。
2. 单击顶部菜单栏杯子，确认立即变成“开”，没有二次确认。
3. 运行 `pmset -g assertions`，确认本工具创建 `KeepAwake - allow locked background tasks` 对应的 `PreventUserIdleSystemSleep`。
4. 再次单击，确认图标变为“关”，本工具对应的系统断言消失。其他应用的断言无需消失，不要结束它们。
5. 验证右键菜单能关闭，关闭菜单后左键仍然直接切换。
6. 用户主动选择时才设置“登录时打开”。退出应用后，不应残留本工具的防休眠断言。

需要命令行诊断时，按 Tests/README.md 执行有 20 秒上限的独立测试。不要将测试通过误写成“图书馆网络已验证不断线”。

## 5. 交付

说明安装位置、开/关方法、登录项选择、卸载步骤，以及实际验证的项目。如果无法看到顶部图标，应查找菜单栏空间、显示位置等原因，不能在未验证时声称图标已可见。

关闭测试开启的防休眠；如果用户已主动开启并正在使用，则保留用户选择并说明当前状态。不要自动锁屏或合盖测试，以免中断用户工作。

强调使用边界：电脑需保持开盖；公共 Wi-Fi 认证过期、网络中断、手动睡眠、低电量和 Agent 自身错误均不由此工具解决。
